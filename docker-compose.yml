services:
  frontend:
    build:
      context: ./frontend
    ports:
      - "8082:80"
    networks:
      - game-network

  backend:
    build:
      context: ./backend
    ports:
      - "8083:8080"
    networks:
      - game-network

networks:
  game-network:
    driver: bridge
